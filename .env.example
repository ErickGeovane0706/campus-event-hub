# OPCIONAL: variaveis do docker-compose.yml.
# So crie o .env (copiando este arquivo) se quiser mudar os valores padrao.
# Se mudar algo aqui, ajuste tambem o backend/.env para o Spring conectar.

DB_NAME=campus_event_hub
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_PORT=5432
